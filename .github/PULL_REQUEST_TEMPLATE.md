## What changed

## Why

## How it was verified

- [ ] `bun run format:check`
- [ ] `bun run lint`
- [ ] `bun run typecheck`
- [ ] `bun run test`

## Operator impact

Does this change what an operator must do when upgrading an existing Installation (new secrets, migrations, token permissions, setup wizard changes)? If so, describe it here and update `docs/operators.md` and `CHANGELOG.md`.
