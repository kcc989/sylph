# Cloudflare agent lifecycle integration

> Historical record for the source and date below. Status, URLs, and commands may be outdated; recorded approvals do not authorize new actions. Use [current documentation](../../README.md).

This follow-up extends the original gap fixes. Installations will deploy fresh, so historical Installation migration is out of scope. Implementation, external starter publication, and live proof have separate checkpoints.

| Area                  | Current implementation                                                                                                                                                    | Required deployed evidence                                                                                                                                                                                       |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Starter               | External template with scoped deployment, managed KV/Queue bindings, registered SQLite object recovery and paused first-release bootstrap                                 | Publish the reviewed compatible commit, pin it and verify the exact deployed source/template identity. New Projects only.                                                                                        |
| Recovery              | Coordinated D1/R2/registered-object snapshots and restore/undo; recoverable KV and Queue journals in application D1; initial object restore drill required before capture | Run real Cloudflare drills and independently compare restored data, object metadata and undo state. Local Workerd tests are not provider proof.                                                                  |
| Production operations | Thirteen-phase product journey includes a real runtime failure, correlated telemetry, incident-linked repair Workspace, checked Preview, acceptance and corrected release | Run the authenticated fresh-Installation journey. Two model Workspaces have separate $4 limits, for a maximum $8 model budget.                                                                                   |
| Resources             | Scoped broker, exact consumer detachment, reviewed removal Workspaces, and a temporary R2 lock experiment to produce actual partial cleanup failure                       | Observe terminal failed cleanup, remove only the test lock, retry through the product and independently verify absence. Full managed Queue retirement and snapshot-bearing namespace deletion remain restricted. |
| Browser               | Existing screenshot and ownership fixes, plus browser actions for the combined journey                                                                                    | Earlier 79-step service evidence remains independent of the new full product run. GitHub OAuth needs its own configured login proof.                                                                             |

[Sanitized follow-up browser evidence](browser-service-followup-smoke.json) records the clean source commit `a91e848909ec9a093c09482ae990317eeeac9738`, source hash and disposable stage. The fixture passed 79 ordered steps, native CRUD, screenshots at 1440×900 and 390×844, coordinate actions, configured OAuth fixture state/code validation, shared session cookies, interruption and cleanup of the browser context. Forbidden-origin popup requests were zero. This is service-fixture evidence, not a combined product deployment result.

Adapter bounds remain subject to the Project resource plan limit of twenty total resources. Worker secret recovery uses the shared Project secret set. R2 configuration checks reject committed-object lifecycle changes, active locks, Sippy and event notifications; the multipart-abort-only default is allowed. Missing or ambiguous policy evidence fails closed.

R2 snapshots are bounded to 10,000 objects, 16 MiB per object and 64 MiB of object bytes per bucket. Encrypted chunks remain below D1 row limits. A recovery group authenticates all saved bucket chunks and checks fresh undo state before restoring any database. Unknown restore outcomes keep writers paused and cannot be replayed as a new successful attempt.

See [combined lifecycle procedure](../../../tests/release-smoke/COMBINED.md) and [starter candidate procedure](../../../tools/template-contract/README.md). No live application restore, resource deletion, earlier Installation migration or production release has been performed by the follow-up integration owner.

## Current local checks

Platform source `8f9a885` passed all 803 tests, formatting, lint and the local Workerd agent-runtime smoke. Build and typechecks passed before the final test-only fixture changes. External starter `33482d3892652237dccfb80a9d2f4e479a6d51e0` passed 59 tests, typecheck, lint, formatting and production build. The platform contract checker also passed, including exact copied-source comparison and recovery schema reconciliation. The user-approved candidate was published on `codex/project-capability-broker`; its remote hash was verified before the immutable pin changed.

These results do not establish fresh deployed journey, application restore/undo, or partial-cleanup proof. The combined source is in draft PR #73 until live evidence is complete.

## Earlier integration checks

The final combined implementation at `635f28e` passed format, lint, all package/root typechecks, the full test command (617 tests), and the production build. The local Workerd runtime smoke and all 59 Storybook browser tests also passed. The starter candidate passed 28 tests, typecheck, lint, formatting, build, the shared platform contract verifier, full patch applicability, and copied recovery-source hash checks.

The Browser Run service fixture passed 74 ordered steps at exact source commit `71b118722d8dd5fc194e43f74ef494d3bd86ec82`, stage `smoke-browser-mtrwecsj`, with a clean source tree and recorded source hash. [Sanitized evidence](browser-service-smoke.json) lists the steps. It covered actual reconnect, blocked unconfigured popups, CRUD/D1 and session persistence, duplicate action receipts, failure/retry, desktop/mobile DOM assertions, stale attempts, human handoff, a separate OAuth fixture with state/code checks, interruption, blocked navigation, and session close. These are service-fixture results; the full Sylph product UI, a real identity provider, and the combined Installation lifecycle were not exercised.

Cloudflare's client alone reproduced screenshot capture timeout after freeze/resume. The successful fixture selected an explicit, audited DOM-only policy. The default screenshot policy still blocks acceptance when capture fails. DOM activation does not verify pointer hit targets or appearance. Disposable fixture stages remain retained; no cleanup was performed.

Integration regression fixes include CI filtering of recovery variables, flattened application secret snapshots, background health scheduling, distinct migration order, recovery source reconciliation, published commit/URL selection after failed releases, exact stateful backup coverage, private verification before writer resume and public verification after it, and monitoring/repair for failed publications without fabricating telemetry.

The approved starter branch was published and its remote hash verified before the immutable pin changed. No production release, application restore/undo, resource deletion, or earlier Installation migration was performed by the integration owner. Disposable browser-fixture evidence remains separate from combined product lifecycle proof.
