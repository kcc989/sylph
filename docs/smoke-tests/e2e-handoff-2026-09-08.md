# E2E verification handoff — 2026-09-08

## Start here

Continue the deployed Sylph lifecycle verification. The E2E vision is **not yet verified**. Do not start the gap audit again or discard the existing Project and Workspace.

PR [#74](https://github.com/kcc989/sylph/pull/74) is open on `codex/verify-merged-lifecycle`. Its tested implementation head is `9ef6ab97796054283763c30f41a508d05baf8e04`; [CI run 34269382806](https://github.com/kcc989/sylph/actions/runs/34269382806) passed all jobs. This handoff is a later documentation-only commit. Refresh GitHub state before continuing; the PR may have merged since this document was written.

The next task is to deploy the unpatched container implementation to the disposable test stage, verify Cursor through the actual product, and resolve the application Check's missing `/workspace/package.json`. Then continue preview, release, failure, repair, recovery, and cleanup verification.

## Non-negotiable decisions

- No third-party patches, package-manager patch declarations, installed source/binary modifications, dependency build rewrites, or copied dependency internals as a workaround. The entire `patches/` directory is deleted. Do not restore the Workerd binary modification, Cursor protocol transform, or custom HTTP/2 connector.
- Cursor may run in a Node container, like Codex. PR #74 implements that with the unmodified community provider. It is not the official Cursor Agent SDK.
- Use the Codex **in-app browser** for product verification so the user can follow. Read the browser skill and discover existing tabs each time; old tab IDs and JavaScript variables do not survive a new chat reliably. Do not launch a separate Playwright browser as a substitute.
- Templates remain an external GitHub repository. Template changes apply only to new Projects. No raw template-upgrade patch or existing-Project upgrade mechanism.
- The user will scrap prior Installations and deploy fresh. Do not restore legacy Installation migration work. Preserve the current disposable test fixture while debugging; this decision is not authorization to delete it immediately.
- Keep `packages/ui` presentational. Use Oxlint and Oxfmt. Follow `AGENTS.md`; no authored source comments.
- Do not infer deployed proof from unit tests, synthetic receipts, direct database edits, or a healthy landing page. Record source identity for each live result.

## Where the work lives

| Item                   | Value                                                                       |
| ---------------------- | --------------------------------------------------------------------------- |
| Working checkout       | `/private/tmp/sylph-remaining-01a07d89/integration`                         |
| Branch                 | `codex/verify-merged-lifecycle`                                             |
| PR                     | https://github.com/kcc989/sylph/pull/74                                     |
| Desktop checkout       | `/Users/caseycollins/.codex/worktrees/8041/sylph`                           |
| Local evidence root    | `/private/tmp/sylph-remaining-01a07d89`                                     |
| Private stage record   | Working checkout + `/.alchemy/smoke-runs/smoke-lifecycle-01a07d89/run.json` |
| Template repository    | `kcc989/sylph-tanstack-template`                                            |
| Published template pin | `33482d3892652237dccfb80a9d2f4e479a6d51e0`, version `0.3.0`                 |

The desktop checkout had unrelated changes to `README.md` and untracked `docs/benchmarks/`. Inspect current status and preserve them. Do not reset that checkout to recover this task. If the temporary checkout is gone, fetch the PR branch into an isolated worktree. This document is committed to GitHub; private deployment state and credentials are not.

## Verified versus pending

| Area                      | Evidence and limit                                                                                                                                                                                                                                                                                  |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Build and dependencies    | Green CI on `9ef6ab9`: frozen install, format, lint, typecheck, full tests, application build, Docker image build, Workerd regression, Storybook, and template contract. No dependency patches.                                                                                                     |
| Cursor container locally  | Built Node 24 Docker image; login endpoint returns PKCE fields and HTTPS URL; unknown routes return 404. Tests cover streaming, cancellation, overlap rejection, size bounds, binary file snapshots, stale-file removal, symlinks, and unsafe paths. No live model call on this implementation yet. |
| OpenCode in Workerd       | Public server/client/profile/plugin APIs. Actual Workerd tests pass initial/recovered plugin registration, native file/search/shell tools, persistence after restart, cache behavior, Check completion/retry, request budget, and compaction.                                                       |
| Existing deployed product | Earlier code demonstrated sign-in, Project/Workspace creation, Cursor discovery, agent file reads/writes, shell execution, persistence after reload, and checkpoints through the in-app browser. These results predate patch removal and do not validate the new container code.                    |
| Application Check         | Latest recorded Check failed installation because `/workspace/package.json` was missing in the CI sandbox. Source file existed and parsed in the agent Workspace. Unresolved.                                                                                                                       |
| Preview and release       | Complete application preview and release journey has not passed.                                                                                                                                                                                                                                    |
| Failure and repair        | Failed Check triggered agent continuation on older code. This is not proof of observing and repairing a deployed application/release failure.                                                                                                                                                       |
| Recovery and cleanup      | Live D1/R2 restore, undo, metadata preservation, partial-cleanup retry, and broader managed-state recovery remain unverified.                                                                                                                                                                       |

The recovery CI fixture needed an explicit 30-second test timeout and one Workerd instance per queue test file with D1 reset between cases. Assertions were retained. The final remote run passed.

## Existing disposable test environment

These are the last recorded live identities, not a new deployment check performed while writing this handoff:

| Item                         | Identity                                                                           |
| ---------------------------- | ---------------------------------------------------------------------------------- |
| Stage                        | `smoke-lifecycle-01a07d89`                                                         |
| Installation URL             | https://sylph-website-smoke-lifecycle-z2jjjljweyxee7ezdk4z52yj.apingot.workers.dev |
| Last deployed Sylph revision | `e198c9007c5ba7f1d01308aeeeac05f00ed0ae09` — older patched implementation          |
| Cloudflare account           | `03e608b74a0500b0b48c9647f5b200ad`                                                 |
| Installation D1              | `d3799a44-7c06-4c3d-a2d8-921f5e89a2a3`                                             |
| Project                      | `746920b9-d33b-44a9-a3ec-61b75a505f74`, slug `combined-lifecycle-proof`            |
| Workspace                    | `39d83a6c-70a5-43ec-a23a-48f09eee1822`                                             |
| OpenCode session             | `ses_f7e481b9effdHLxbeRmjcJ9uwG`                                                   |
| Provider/model               | User-connected Cursor, `grok-4.6`                                                  |

Reuse the authenticated in-app browser session if available. A second Workspace, `gentle-heron`, exists but is not the main fixture. The original OpenRouter smoke budget was $4 and exhausted; do not reset it, increase it, or switch paid providers silently. The user added Cursor and explicitly asked us to continue testing with it.

## Application fixture and blocking Check

The agent created a todo/D1 and object/R2 fixture through the product. Files include `migrations/0004_todos.sql`, todo/object functions and tests, resource-plan scripts, and a release-failure wrapper. The existing fixture is useful work, not an empty Project to recreate.

1. Checkpoint `b7166fe82e72cd4ec69aa0ce58daf9caa3c7a616`, Check `check-af55d33c-acbb-4476-a4d5-3e1a41ac1e02`: installation failed because `package.json` contained a literal SDK read-output envelope beginning with `Read file`. The first-party Cursor read adapter was corrected; the agent repaired affected files.
2. Checkpoint `cc7c5b881ce52e65640208933c50ec2b6a20d10f`, Check `check-05f31f23-e032-4def-8973-4c96fce5822c`: source audit found zero remaining read envelopes and `package.json` parsed, but CI installation failed with `npm ENOENT /workspace/package.json`.
3. A checkpoint attempt also received Git remote refs HTTP 500; retry succeeded. That transient service failure was not systematically fixed.

Do not ask the agent to rewrite `package.json` without first tracing the missing CI checkout. Inspect the Check command output and the unmodified Cloudflare CI integration's checkout/materialization path, working directory, and public command APIs. Earlier patches included command behavior; they are now prohibited. Fix Sylph's integration if necessary.

## Exact continuation order

1. Read this file, `AGENTS.md`, and [Cursor architecture](cursor-provider.md). Inspect git status and refresh PR/main/CI state. Keep fixes in the existing PR if still open; if merged, branch from current main. Do not republish the unchanged template.
2. Inspect the private stage record and actual `/__sylph/smoke-identity`. Do not confuse the local branch with deployed source. Preserve the old run record before an update.
3. Deploy the current unpatched code to the existing disposable stage for continuity. Use Alchemy v2 and Node 24. Verify the resulting identity against the exact committed source and template pin. Check that the new Cursor container image/binding is present. Do not mark an updated stage as a fresh-Installation proof.
4. Open the existing Workspace in the in-app browser. Verify Cursor can read an existing file, make a small controlled edit, run a command, checkpoint, and continue after tool results. Verify cancellation and a separate session do not interfere. Retain the fixture's actual tool results and Check IDs.
5. Trace and fix the missing CI `/workspace/package.json`, then run a new Check through the product. A local build is not a substitute. Confirm the Check fetched the intended checkpoint, installed dependencies, ran tests/build, and produced usable evidence.
6. Complete the real preview and application-user journey: login, todo create/edit/delete/reload, R2 object round-trip, checkpoint identity, and browser inspection. Check a second Preview for isolation where the combined scenario requires it.
7. Release through the actual product. Observe a controlled failure using the fixture's release guard and product controls. Verify the failure is visible, previous production remains usable as appropriate, and repair plus release succeeds. Do not inject database status rows or synthetic success receipts.
8. Exercise live D1/R2 restore and undo with before/after records, R2 bytes and metadata, and application-user behavior. Verify managed KV, journaled Queue replay, and registered Durable Object recovery within their actual supported contracts. Do not claim arbitrary application KV, transport Queue contents, or arbitrary Durable Object state is backed up.
9. Verify ownership/resource lifecycle boundaries and an actual partial cleanup failure followed by product retry. Confirm provider-visible outcomes, retained data, and metadata. API access denial is not evidence that a resource is absent. If a real failure cannot be induced safely through the available controls, record that specific blocker.
10. After the fixes work, run a genuinely fresh Installation on the final source to prove `login → Project → agent changes → preview → release → failure → repair`. Preserve the current stage until that proof and any requested cleanup are complete. Report each unverified step explicitly.

## Commands and credential handling

Read [release-smoke instructions](../../tests/release-smoke/README.md) and [combined proof](../../tests/release-smoke/COMBINED.md) before deployment. Their historical Playwright, budget, template, and migration notes do not override the user's decisions above.

For local validation from the selected clean checkout:

```sh
bun install --frozen-lockfile
bun run format:check
bun run lint
bun run typecheck
bun run test
bun run build
bun run smoke:runtime
docker build -f packages/cursor-provider/Dockerfile -t sylph-cursor:test .
```

For a new magic-auth stage, the supported entry points are `bun run smoke:release:doctor -- --auth magic` and `bun run smoke:release:deploy -- --auth magic`; see the combined runbook for source/template arguments. A new stage requires new sign-in and does not reuse the existing product session. Magic auth does not prove GitHub OAuth.

The runner loads `~/.config/sylph/release-smoke.env`. The existing stage's private `deploy.env` path is recorded in `run.json`. Never print, commit, or source these files, export their contents to chat, or copy credentials into a checkout. Reuse the configured GitHub App/proxy; do not create replacements. Node 24 is available locally at `/Users/caseycollins/.nvm/versions/node/v24.16.0/bin` if still installed.

The user approved deployment of the disposable stage, updated Cloudflare token permissions including Queues Write, approved the published template revisions, and approved Cursor testing. Those decisions are recorded context, not a reason to repeat setup questions. They do not authorize unrelated production deployment, destruction, or arbitrary paid spend. Follow applicable action-specific confirmation requirements for credentials, restore/undo, or destructive actions; explain any remaining approval boundary precisely.

Existing private `prepare-read-output-deploy.mjs` and `resume-read-output-deploy.mjs` illustrate the last stage update, source check, private environment forwarding, and deployed identity verification. Inspect them rather than blindly rerunning them: they have old labels, `flag: "wx"` backups, and source-specific assumptions. Do not overwrite the previous evidence or alter a run record to claim deployment before Alchemy and identity verification succeed.

## Evidence to preserve and refresh

Under `/private/tmp/sylph-remaining-01a07d89`:

- `cursor-live-evidence.json` records older browser observations. Its final Check status still says `running`; that snapshot predates the later missing-package failure described above.
- `read-check-state.mjs` and `read-stream-errors.mjs` are read-only diagnostic helpers. Inspect their query/time-window and identity assumptions before use; their outputs can be sensitive.
- `container-build.log`, `container-tests-final.log`, `container-typecheck.log`, `container-lint.log`, `container-format.log`, and `unpatched-runtime-final.log` hold local checks.
- `cursor-docker-build.log` and `container-focused-final.log` hold container evidence. The disposable local Docker probe was stopped after verification.
- `container-ci-verification.log` and `container-ci-final-failure.log` are failed intermediate runs. Final successful CI is the linked GitHub run, not these logs.
- The stage's `.alchemy/smoke-runs/smoke-lifecycle-01a07d89/` directory holds private deployment logs, environment snapshots, and lifecycle attempts. Preserve it locally; do not publish it wholesale.

For every next live result, record source and template commits, stage, Project/Workspace, application checkpoint, Check/Workflow IDs, observed product action, application data before/after, and pass/fail/blocker. Keep credentials out of committed evidence. Older files in `docs/smoke-tests/` are historical and may mention discarded patches, migrations, pins, or browser procedures; this handoff takes precedence for continuation.
