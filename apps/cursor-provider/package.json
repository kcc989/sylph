{
  "name": "@workspace/cursor-provider",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/server.ts --target=node --external cursor-opencode-provider --external 'cursor-opencode-provider/*' --outfile dist/server.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@workspace/domain": "workspace:*",
    "cursor-opencode-provider": "0.6.6",
    "effect": "4.0.0-rc.112"
  },
  "devDependencies": {
    "@types/node": "^26",
    "@types/bun": "1.4.0",
    "typescript": "^6"
  }
}
